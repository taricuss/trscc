[build-system]
requires = ["setuptools>=68.0", "wheel>=0.40"]
build-backend = "setuptools.build_meta"

[project]
name = "trcss"
version = "1.0.0"
description = "TRCSS: Transcription-Replication Context Score for prime editing efficiency prediction"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [
  { name = "TRCSS Contributors" }
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Science/Research",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Topic :: Scientific/Engineering :: Bio-Informatics",
]
keywords = [
  "prime-editing",
  "crispr",
  "genome-editing",
  "bioinformatics",
  "replication-transcription-conflict",
  "fork-directionality",
  "ok-seq",
]
dependencies = [
  "numpy>=1.24.0",
  "pandas>=2.0.0",
  "scipy>=1.10.0",
  "pyBigWig>=0.3.22",
  "bioframe>=0.6.0",
]

[project.optional-dependencies]
analysis = [
  "scikit-learn>=1.3.0",
  "statsmodels>=0.14.0",
  "matplotlib>=3.7.0",
  "seaborn>=0.12.0",
  "joblib>=1.3.0",
  "tqdm>=4.65.0",
  "pyyaml>=6.0.1",
  "requests>=2.31.0",
]
dev = [
  "pytest>=7.4",
  "pytest-cov>=4.1",
]

[project.urls]
Homepage = "https://github.com/example/trcss"
Documentation = "https://github.com/example/trcss#readme"
Repository = "https://github.com/example/trcss"
Zenodo = "https://doi.org/10.5281/zenodo.XXXXX"

[project.scripts]
trcss-compute = "trcss.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
trcss = ["py.typed"]
